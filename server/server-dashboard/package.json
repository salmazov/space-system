{
  "name": "space-system-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && squint compile $(find src -name '*.cljs' | sort) --paths src --output-dir dist --extension js && cp -R ../../node_modules/squint-cljs dist/squint-cljs"
  },
  "devDependencies": {
    "squint-cljs": "^0.11.189"
  }
}
