{
  "name": "space-system-bot-fleet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && nbb --classpath src -e \"(require 'bot-fleet.main)\"",
    "dev": "nbb --classpath src -m bot-fleet.main",
    "start": "nbb --classpath src -m bot-fleet.main"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "nbb": "^1.4.207"
  }
}