{
  "name": "space-system-bot-government",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && nbb --classpath src -e \"(require 'bot-government.main)\"",
    "dev": "nbb --classpath src -m bot-government.main",
    "start": "nbb --classpath src -m bot-government.main",
    "test": "nbb --classpath src:test -m bot-government.test-runner"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "nbb": "^1.4.207"
  }
}
